let nombres = (5, 10, 15, 20, 25);
console.log ("Tableau des nombres : ", nombres);
let somme = 0;
for (let i = 0; i < nombres.length; i++) {
    somme += nombres[i];
    }
    console.log('Le total est de ' + somme);